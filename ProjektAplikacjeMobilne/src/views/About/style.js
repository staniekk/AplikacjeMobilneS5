import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({

    textInfoTop: {
        color: '#000',
        fontWeight: 'normal',
        textAlign: 'center',
        fontSize: 40,
        flex: 1,
        paddingTop: 90,
        justifyContent: 'flex-start'
    }

})
